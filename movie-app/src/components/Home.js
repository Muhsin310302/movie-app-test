/*import { useState } from 'react'
import './App.css';
import axios from 'axios'

function App() {
/*  const [movie, setMovie] = useState([])
  const fetchMovie = async() => {
    const film = await fetch('https://www.omdbapi.com/?s=james-bond&apikey=3f4fa097')
  }

  const [movie, setMovie] = useState([])

  const fetchMovie =() =>{
    axios.get('https://www.omdbapi.com/?s=james-bond&apikey=3f4fa097')
    .then((response) =>{
      console.log(response);
      setMovie(response.data.Search)
    })
  } 
  return (
    <>
    <button onClick={fetchMovie}>fetch movie</button>

    {
      movie.map((value, index) =>{
        return(
          <h3 key={index}>{value.Title}</h3>
        )
      })
    }
    </>
  );
}

export default App;*/
